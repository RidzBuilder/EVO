# EVO Production Baseline — v1.0

STATUS: LOCKED

SCOPE
Ontology; Canonical IR; input inspection and semantic mapping; validation; capability registry/resolution; constraint resolution; execution planning; provider compiler/adapter boundary; runtime/job boundary; provenance; diagnostics; versioning and governance.

REFERENCE IMPLEMENTATION
TypeScript reference core. Python, LLM frameworks, audio/CV libraries, cloud render APIs and frontend frameworks remain replaceable integrations.

PROVIDER READINESS
A provider integration is production-ready only after satisfying the adapter contract and conformance tests. EVO Core has no single-provider dependency.